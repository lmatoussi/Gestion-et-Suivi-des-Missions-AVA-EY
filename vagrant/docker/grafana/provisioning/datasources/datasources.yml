apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true

  - name: Seq
    type: seq
    access: proxy
    url: http://seq:80
    editable: true
